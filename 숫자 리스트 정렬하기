#bubble_sort 함수 정의해주기
def bubble_sort(a):
    #n에 입력값 요소 갯수 지정
    n = len(a)
    #밑의 과정을 n번 반복한다
    for i in range(n - 1):
     # j가 0에서 n-1일때까지 반복한다
     for j in range(n - 1):
            # 만약 입력값의 j번째 요소가 j+1번째 요소보다 크면
            if a[j] > a[j + 1]:
               #두 요소의 순서를 바꾼다
                a[j], a[j + 1] = a[j + 1], a[j]
    #정렬된 입력값 반환
    return a
#함수 실행 후 출력
print(bubble_sort([5, 3, 1, 4, 2]))
