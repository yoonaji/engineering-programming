#문제1: HTML 태그 제거하기 함수명: remove_html_tags 설명: 주어진 HTML 문자열에서 HTML 태그를 모두 제거하는 함수를 작성 입력:

#Hello, World!

#출력: 'Hello, World!'
#remove_html_tags 함수 지정하기
def  remove_html_tags(n):
  #re 함수 불러오기
  import re
  #p에 <>사이에 모든게 들어갈 수 있지만 ?로 non-greedy를 만들어주고 컴파일한다.
  p = re.compile('<.*?>')
  #n을 p로 컴파일하고 매치되는 <.*?>을 ""로 바꿔 없애준 후 a에 저장한다.
  a = p.sub("",n)
  #a 값을 반환한다.
  return a

#함수 실행하기
print(remove_html_tags('<p>Hello, World!</p>'))
